# Required: Rust API base URL (no trailing slash)
API_BASE_URL=http://127.0.0.1:8080

# Optional today (backend does not validate yet); required once feature/api-auth
# is merged on the Rust side. Format: bhv_<env>_<hex-32>
# API_KEY=
